---
title: "Hanzo Studio(portable) Windows"
description: "This tutorial will guide you on how to download and start using Hanzo Studio Portable and run the corresponding programs"
icon: "box"
sidebarTitle: "Portable(Windows)"
---

import FirstGeneration from "/snippets/install/first-generation.mdx"
import AddExternalModels from "/snippets/install/add-external-models.mdx"

**Hanzo Studio Portable** is a standalone packaged complete Hanzo Studio Windows version that has integrated an independent **Python (python_embeded)** required for Hanzo Studio to run. You only need to extract it to use it. Currently, the portable version supports running through **Nvidia GPU** or **CPU**.

This guide section will walk you through installing Hanzo Studio Portable.

## Download Hanzo Studio Portable

You can get the latest Hanzo Studio Portable download link by clicking the link below

<a className="prose" href="https://github.com/hanzoai/studio/releases/latest/download/Hanzo Studio_windows_portable_nvidia.7z" style={{ display: 'inline-block', backgroundColor: '#0078D6', color: '#ffffff', padding: '10px 20px', borderRadius: '8px', borderColor: "transparent", textDecoration: 'none', fontWeight: 'bold'}}>
  <p className="prose" style={{ margin: 0, fontSize: "0.8rem" }}>Download Hanzo Studio Portable</p>
</a>

After downloading, you can use decompression software like [7-ZIP](https://7-zip.org/) to extract the compressed package

The file structure and description after extracting the portable version are as follows:

```
Hanzo Studio_windows_portable
├── 📂Hanzo Studio                   // Hanzo Studio main program
├── 📂python_embeded            // Independent Python environment
├── 📂update                    // Batch scripts for upgrading portable version
├── README_VERY_IMPORTANT.txt   // Hanzo Studio Portable usage instructions in English
├── run_cpu.bat                 // Double click to start Hanzo Studio (CPU only)
└── run_nvidia_gpu.bat          // Double click to start Hanzo Studio (Nvidia GPU)
```

## How to Launch Hanzo Studio

Double click either `run_nvidia_gpu.bat` or `run_cpu.bat` depending on your computer's configuration to launch Hanzo Studio. 
You will see the command running as shown in the image below

![Hanzo Studio Portable Command Prompt](/images/hanzo-studio-portable-cmd.png)

When you see something similar to the image
```
To see the GUI go to: http://127.0.0.1:8188
```
At this point, your Hanzo Studio service has started. Normally, Hanzo Studio will automatically open your default browser and navigate to `http://127.0.0.1:8188`. If it doesn't open automatically, please manually open your browser and visit this address.

<Warning>During use, please do not close the corresponding command line window, otherwise Hanzo Studio will stop running</Warning>

<AddExternalModels/>

<FirstGeneration/>

## Additional Hanzo Studio Portable Instructions

### 1. Upgrading Hanzo Studio Portable

You can use the batch commands in the update folder to upgrade your Hanzo Studio Portable version

```
Hanzo Studio_windows_portable
└─ 📂update
   ├── update.py
   ├── update_comfyui.bat                          // Update Hanzo Studio to the latest commit version
   ├── update_hanzo_studio_and_python_dependencies.bat  // Only use when you have issues with your runtime environment
   └── update_hanzo_studio_stable.bat                   // Update Hanzo Studio to the latest stable version
```

### 2. Setting Up LAN Access for Hanzo Studio Portable

If your Hanzo Studio is running on a local network and you want other devices to access Hanzo Studio, you can modify the `run_nvidia_gpu.bat` or `run_cpu.bat` file using Notepad to complete the configuration. This is mainly done by adding `--listen` to specify the listening address.
Below is an example of the `run_nvidia_gpu.bat` file command with the `--listen` parameter added

```bat
.\python_embeded\python.exe -s Hanzo Studio\main.py --listen --windows-standalone-build
pause
```

After enabling Hanzo Studio, you will notice the final running address will become
```
Starting server

To see the GUI go to: http://0.0.0.0:8188
To see the GUI go to: http://[::]:8188
```

You can press `WIN + R` and type `cmd` to open the command prompt, then enter `ipconfig` to view your local IP address. Other devices can then access Hanzo Studio by entering `http://your-local-IP:8188` in their browser.

